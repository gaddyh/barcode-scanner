# Local dev: Vite runs on :5173, API on :8000 — set this so the frontend
# knows where the API is.
# Docker/Render: leave this UNSET — the frontend and API are same-origin,
# so a relative URL is used automatically.
VITE_API_BASE_URL=http://localhost:8000
